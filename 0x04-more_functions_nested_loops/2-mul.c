/**
 * mul - Funtion to compute the product of two numbers
 * @a: first number
 * @b: second parameter
 * Return: the product of the two
 */
int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
