The Second Task I haven't done yet

with task 1, I created three files;
1, a main.h header file to store the function prototypes in order to prevent errors when there is a number of func declarations greater than one in the main file
2. the file to store the funtion and its contents
3. the file to run the full code containing the header files (which when included declares all functions) and the main func

